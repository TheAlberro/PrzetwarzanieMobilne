<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aplikacja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplikacja">

        <activity
            android:name=".ShowActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Dashboard" />
        <activity android:name=".Register" />
        <activity android:name=".CreateEvent" />
        <activity android:name=".Chat" />
        <activity android:name=".ShowActivityFriend" />



        <activity android:name="com.example.aplikacja.Friends" />
        <activity android:name="com.example.aplikacja.FriendRequest" />
        <activity android:name="com.example.aplikacja.friendCallendar" />
        <activity android:name="com.example.aplikacja.CalendarView" />




        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>